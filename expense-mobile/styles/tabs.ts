import { StyleSheet } from 'react-native';

export const tabsColors = {
  active: '#2563EB',
  inactive: '#8E8E93',
};

export const tabsStyles = StyleSheet.create({
  tabBar: {
    height: 95,
    paddingTop: 6,
    paddingBottom: 8,
  },

  tabBarLabel: {
    fontSize: 12,
  },
});